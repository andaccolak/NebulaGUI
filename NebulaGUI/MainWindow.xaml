<Window x:Class="NebulaGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:charts="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf" 
        xmlns:local="clr-namespace:NebulaGUI" xmlns:vm="clr-namespace:NebulaGUI.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- ViewModel instance'larını oluşturmak için Resource tanımları -->
        <vm:MainViewModel x:Key="vm1"/>

    </Window.Resources>

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="161*"/>
            <RowDefinition Height="159*"/>
            <RowDefinition Height="164*"/>
        </Grid.RowDefinitions>

        

        <charts:CartesianChart  Zoom="X" DataContext="{StaticResource vm1}" DisableAnimations="True" Series="{Binding AltitudeSeries}" LegendLocation="Bottom" Margin="443,16,120,10" Grid.Row="2"/>

        <charts:CartesianChart  Zoom="X" DataContext="{StaticResource vm1}" DisableAnimations="True" Series="{Binding TemperatureSeries}" LegendLocation="Bottom" Margin="380,17,174,16" Grid.Row="1"/>
        <charts:CartesianChart  Zoom="X" DataContext="{StaticResource vm1}" DisableAnimations="True" Series="{Binding VoltageSeries}" LegendLocation="Bottom" Margin="26,5,530,15" Grid.Row="1"/>
        <charts:CartesianChart  Zoom="X" DataContext="{StaticResource vm1}" DisableAnimations="True" Series="{Binding SpeedSeries}" LegendLocation="Bottom" Margin="779,93,22,22" Grid.Row="1" Grid.RowSpan="2">
        </charts:CartesianChart>
        <charts:CartesianChart  Zoom="X" DataContext="{StaticResource vm1}" DisableAnimations="True" Series="{Binding PressureSeries}" LegendLocation="Bottom" Margin="47,5,471,15" Grid.Row="2">
        </charts:CartesianChart>


        <ListView ItemsSource="{Binding AllDatasReversed}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="PaketNo" DisplayMemberBinding="{Binding  PaketNo}"/>
                    <GridViewColumn Header="UyduStatusu" DisplayMemberBinding="{Binding  UyduStatusu}"/>
                    <GridViewColumn Header="HataKodu" DisplayMemberBinding="{Binding HataKodu}"/>
                    <GridViewColumn Header="GondermeSaati" DisplayMemberBinding="{Binding GondermeSaati}"/>
                    <GridViewColumn Header="Basinc1" DisplayMemberBinding="{Binding Basinc1}"/>
                    <GridViewColumn Header="Basinc2" DisplayMemberBinding="{Binding Basinc2}"/>
                    <GridViewColumn Header="Yukseklik1" DisplayMemberBinding="{Binding Yukseklik1}"/>
                    <GridViewColumn Header="Yukseklik2" DisplayMemberBinding="{Binding Yukseklik2}"/>
                    <GridViewColumn Header="IrtifaFarki" DisplayMemberBinding="{Binding IrtifaFarki}"/>
                    <GridViewColumn Header="InişHizi" DisplayMemberBinding="{Binding InişHizi}"/>
                    <GridViewColumn Header="Sicaklik" DisplayMemberBinding="{Binding Sicaklik}"/>
                    <GridViewColumn Header="PilGerilimi" DisplayMemberBinding="{Binding PilGerilimi}"/>
                    <GridViewColumn Header="GpsLatitude" DisplayMemberBinding="{Binding GpsLatitude}"/>
                    <GridViewColumn Header="GpsLongitude" DisplayMemberBinding="{Binding GpsLongitude}"/>
                    <GridViewColumn Header="GpsAltitude" DisplayMemberBinding="{Binding GpsAltitude}"/>
                    <GridViewColumn Header="Pitch" DisplayMemberBinding="{Binding Pitch}"/>
                    <GridViewColumn Header="Roll" DisplayMemberBinding="{Binding Roll}"/>
                    <GridViewColumn Header="Yaw" DisplayMemberBinding="{Binding Yaw}"/>
                    <GridViewColumn Header="RHRH" DisplayMemberBinding="{Binding RHRH}"/>
                    <GridViewColumn Header="IoTData" DisplayMemberBinding="{Binding IoTData}"/>
                    <GridViewColumn Header="Takımno" DisplayMemberBinding="{Binding TakimNo}"/>

                </GridView>
            </ListView.View>
        </ListView>
        <TextBox x:Name="komut" Margin="698,51,34,61" Grid.Row="1">0</TextBox>
    </Grid>
</Window>
